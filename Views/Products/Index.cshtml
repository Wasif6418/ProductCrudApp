@model IEnumerable<ProductCrudApp.Models.Product>

<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

<div class="container mt-4">
    <h1>Products</h1>
    <a class="btn btn-success mb-2" href="/Products/Create">Add New Product</a>

    @if (!Model.Any())
    {
        <p>No products found.</p>
    }
    else
    {
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Name</th>
                    <th>Price</th>
                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            @foreach (var product in Model)
            {
                <tr>
                    <td>@product.Name</td>
                    <td>@product.Price</td>
                    <td>
                        <a class="btn btn-primary btn-sm" href="/Products/Edit/@product.Id">Edit</a>
                        <a class="btn btn-danger btn-sm" href="/Products/Delete/@product.Id">Delete</a>
                    </td>
                </tr>
            }
            </tbody>
        </table>
    }
</div>
